# Postgres connection string (Neon, Supabase, Railway, RDS...)
DATABASE_URL="postgresql://user:password@host:5432/hlpl?sslmode=require"
# Session signing key. Generate with: openssl rand -base64 32
AUTH_SECRET="replace-me"
